
        terminal_colour()

        名稱：
                terminal_colour() 在字符串中替換一段編碼

        語法：
                string terminal_colour(string str, mapping m,
                        int wrap, int indent);
                字符串 terminal_colour(字符串 str，映射 m,
                        整數 wrap, 整數 indent )

        用法：
                terminal_colour() 將字符串 str 中所有匹配 %^key%^
                的地方替換為 value，其中 key 和 value 為映射參數
                m 中的索引和數值。

                下面這段話不好翻譯：
                'wrap' is the optional column number to wrap at,
                and 'indent' is the amount to indent the second
                and following lines.  Codes are assumed to change
                the mode of the terminal and not generate any
                printable characters for the purposes of wrapping.

        這個函數需要在 options 文件裡定義了 PACKAGE_CONTRIB .

        by 發現號(Find@tx)

