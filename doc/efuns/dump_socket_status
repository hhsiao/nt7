
        dump_socket_status()

        名稱：
                dump_socket_status() 顯示出每一個 LPC 套接口的狀態

        語法：
                string dump_socket_status();
                字符串 dump_socket_status()

        說明：
                此函數已取消，由 socket_status 函數代替。

        by 發現號(Find@tx)

