
        db_close()

        名稱：
                db_close 關閉一個已經打開的數據庫連接

        語法：
                int db_close(int handle)
                整數 db_close(整數 handle)

        用法：
                關閉一個已經打開的數據庫，參數 handle 是一個已經
                打開的數據庫的句柄。

                返回值：0 關閉失敗； 1 關閉成功

        by 發現號(Find@tx)



