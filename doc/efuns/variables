
        variables()

        名稱：
                variables() 列出指定物件中的所有變量

        語法：
                string *variables(object, int default: 0);
                字符串陣列 variables(物件， 整數 默認為 0)

        用法：
                variables() 的返回值由兩種不同的情況。
                當第二個參數為零(默認就是 0)，函數返回第一個
                參數所制定的物件中的所有變量名的一個字符串陣
                列。
                當第二個參數為非零值時，會返回更多的信息，
                每一個陣列元素會像下面這個格式：

                        ({ 變量名, 變量類型 }). 

                其中"變量類型"為變量的 LPC 類型，如 "string"
                或者 "protected int" 等。

        這個函數需要在 options 文件裡定義了 PACKAGE_CONTRIB .

        by 發現號(Find@tx)


