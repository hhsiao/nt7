
        db_commit()

        名稱：
                db_commit()

        語法：
                int db_commit(int handle)
                整數 db_commit(整數 handle)

        用法：
                MySQL 不支持 commit

        by 發現號(Find@tx)



