
        query_notify_fail()

        名稱：
                query_notify_fail() 判定一個互動物件是否已設定了
                        待處理的 notify_fail() 信息

        語法：
                mixed query_notify_fail(void);
                混合 query_notify_fail(無參數)

        用法：
                這個函數判斷是否 this_player() 的 notify_fail
                值已經被設定。這個值即可能是一個字符串，也可能
                是一個函數，取決於對 this_player() 最後一次呼叫
                notify_fail() 函數所設定的值。

        此函數需要編譯 MudOS 時在配置文件裡定義了 PACKAGE_CONTRIB。
        同時 NO_ADD_ACTION 沒有被定義。

        參考：
                add_action,
                notify_fail

        by 發現號(Find@tx)


