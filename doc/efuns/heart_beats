
        heart_beats

        名稱：
                heart_beats()
                返回當前有心跳的物件陣列

        語法：
                object array heart_beats()
                *object heart_beats()
                物件陣列 heart_beats（無參數）

        用法：
                返回當前有心跳的所有物件，這個函數只有
                再定義了 PACKAGE_CONTRIB 才可用。

        參考：
                heart_beat,
                set_heart_beat,
                query_heart_beat

        by 發現號(Find@tx)

