     apply(3)               MudOS (5 Sep 1994)                apply(3)
        函數名稱:
                apply() - 處理函數指標 (function pointer).
        函數語法:
                mixed apply( mixed f, mixed *args )
                混合 apply( 混合 f, 混合 *args )
        函數用法:
                如果 f 是一個函式, 就會呼叫 f 並用陣列 args 當作 f
                的參數 (argument).
                □例:
                        apply( f, ({ 1, 2, 3 }) ); 與
                        evaluate( f, 1, 2, 3 ); 是相同的.
