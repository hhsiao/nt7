
        remove_interactive()

        名稱：
                remove_interactive() 將一個互動物件斷線。

        語法：
                int remove_interactive(object);
                整數 remove_interactive(物件);

        用法：
                如果參數指定的物件是互動的，並且沒有被摧毀，
                則將其斷線（失去互動狀態）。
                如果成功則返回 1，返回 0 表示失敗。

        此函數需要編譯 MudOS 時在配置文件裡定義了 PACKAGE_CONTRIB。

        by 發現號(Find@tx)

