
        db_fetch()

        名稱：
                db_fetch() 返回最後一次 db_exec 函數操作的返回值
                        的陣列。

        語法：
                array db_fetch(int db_handle, int row)
                陣列 db_fetch(整數 handle, 整數 row)

        用法：
                此函數返回最後一次 db_exec 函數操作的返回值的陣列。
                handle 為進行操作的數據庫的句柄。
                row 行數。結果的總行數為 db_exec 的返回值。

        by 發現號(Find@tx)



