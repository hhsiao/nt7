        函數名稱:
                all_previous_objects() - 傳回所有呼叫目前函式的物件陣列.
        函數語法:
                object *all_previous_objects();
        函數用法:
                傳回一個所有呼叫目前函式的物件陣列 (an array of object).
                注意, 區域函式 (local function) 的呼叫不會更動目前的物件
                (current object) previous_object()  的內容.
        參考函數:
                call_other(3), call_out(3), origin(3), previous_object(3)
