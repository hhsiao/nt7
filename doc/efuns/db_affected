
        db_affected()

        名稱：
                db_affected() 返回最後發生改變的行的數量

        語法：
                int db_affected(int handle)
                整數 db_affected(整數 handle)

        用法：
                參數 handle 是一個已經打開的數據庫的句柄。

                返回值：這個函數返回最後一次 INSERT、UPDATE 或者
                DELETE 語句所影響的行的數目

        by 發現號(Find@tx)



