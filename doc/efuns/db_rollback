
        db_rollback()

        名稱：
                db_rollback()

        語法：
                int db_rollback(int handle)
                整數 db_rollback(整數 handle)

        用法：
                MySQL 不支持 rollback

        by 發現號(Find@tx)



